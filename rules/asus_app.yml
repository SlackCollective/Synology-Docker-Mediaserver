http:
  routers:
    asus-rtr:
      rule: "HostHeader(`asus.domain.com`)" 
      entryPoints:
        - https
      middlewares:
        - middlewares-oauth
      service: asus-svc
      tls:
        certResolver: dns-cloudflare
  services:
    asus-svc:
      loadBalancer:
        servers:
          - url: "http://xxx.xxx.xxx.xxx:80"  # or whatever your external host's IP:port is 
